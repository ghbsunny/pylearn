#这是个小的flask的例子，是head first python第五章节的内容
#将目录eg_flask 直接下载到本地，运行vsearch4web.py的脚本，运行成功后，在浏览器上进行访问
#链接为 http://ip:port  ，注意，这里的端口可以在代码中指定
#两种结果的记录方式，可以将结果记录到mysql数据库里或者是将结果记录到文本里。这里将mysql的密码和服务器ip做了处理
